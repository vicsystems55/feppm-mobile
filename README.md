# FEPPM Mobile

Flutter Android/iOS application for FEPPM technicians.

## Development

```powershell
flutter pub get
flutter run
```

Run `flutter analyze` and `flutter test` before committing changes.
